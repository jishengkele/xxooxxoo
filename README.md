入口
```
mkdir -p /usr/local/sbin && curl -fsSL https://raw.githubusercontent.com/jishengkele/xxooxxoo/main/incus-egress-switch-wg.sh -o /usr/local/sbin/sbout && chmod +x /usr/local/sbin/sbout && sbout
```

WG部署
```
mkdir -p /usr/local/sbin && curl -fsSL https://raw.githubusercontent.com/jishengkele/xxooxxoo/main/wireguard-egress-server.sh -o /usr/local/sbin/wgout && chmod +x /usr/local/sbin/wgout && wgout
```
